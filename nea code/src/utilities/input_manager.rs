#[derive(Default)]
pub struct Inputs{
    pub mouse_pos: [f32;2],
    pub left_mouse: bool,
    pub right_mouse: bool,
    pub up: bool,
    pub down: bool,
    pub left: bool,
    pub right: bool,
    pub plus: bool,
    pub minus: bool,
}